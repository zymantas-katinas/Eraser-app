{
  "name": "backend",
  "version": "1.0.0",
  "description": "eraser minute App",
  "main": "server.js",
  "scripts": {
    "build": "cd client && npm run build",
    "client-install": "cd client && npm install",
    "start": "nodemon server.js",
    "client": "cd client && npm start",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm run client-install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "config": "^3.3.1",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.9.6"
  }
}
